# Deployment

Vercel project: `motngum-cafe`, production domain: `https://motngum.cafe`.
Pushes to `main` trigger production builds. Verify `npm run build`, then inspect the
deployment logs and smoke-test `/`, `/api/domains`, `/api/quotes`, and `/api/orders`.
