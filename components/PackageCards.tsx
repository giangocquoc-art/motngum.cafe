"use client";
import { useState } from "react";
const packages = [{ name: "Bắt đầu", price: "4.900.000đ", text: "Một website gọn, đủ để khách tìm thấy và tin bạn.", items: ["Landing page chuyển đổi", "Tên miền năm đầu", "Email doanh nghiệp"] }, { name: "Ra mắt", price: "8.900.000đ", text: "Bộ mặt chỉn chu cho thương hiệu đang lớn lên.", items: ["Website 5 trang", "Tên miền năm đầu", "5 hộp thư doanh nghiệp"], featured: true }, { name: "Đi xa hơn", price: "14.900.000đ", text: "Một nền tảng bán hàng có thể tiếp tục phát triển.", items: ["Website bán hàng", "Cấu hình theo yêu cầu", "Bàn giao và hướng dẫn"] }];
export default function PackageCards() { const [selected, setSelected] = useState("Ra mắt"); const [sent, setSent] = useState(""); const [form, setForm] = useState({ name: "", email: "" }); const ids: Record<string, string> = { "Bắt đầu": "start", "Ra mắt": "launch", "Đi xa hơn": "grow" }; async function submit(event: React.FormEvent) { event.preventDefault(); const response = await fetch("/api/orders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, packageId: ids[selected] }) }); const data = await response.json(); setSent(response.ok ? `Đã nhận yêu cầu ${data.order.id}.` : data.message); } return <><div className="package-grid">{packages.map((item) => <article className={`package-card ${item.featured ? "featured" : ""} ${selected === item.name ? "selected" : ""}`} key={item.name}>{item.featured && <span className="package-badge">Được chọn nhiều</span>}<p className="package-kicker">Gói {item.name}</p><h3>{item.price}</h3><p>{item.text}</p><ul>{item.items.map((x) => <li key={x}>{x}</li>)}</ul><button type="button" onClick={() => setSelected(item.name)}>{selected === item.name ? "Đã chọn" : "Chọn gói này"}</button></article>)}</div><form className="order-form" onSubmit={submit}><p className="package-kicker">Gửi yêu cầu — {selected}</p><div><input required aria-label="Tên của bạn" placeholder="Tên của bạn" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /><input required type="email" aria-label="Email" placeholder="Email nhận tư vấn" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /><button type="submit">Gửi yêu cầu →</button></div>{sent && <p role="status">{sent}</p>}</form></>; }
