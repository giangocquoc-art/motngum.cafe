"use client";
import { useState } from "react";
const packages = [{ name: "Bắt đầu", price: "4.900.000đ", text: "Một website gọn, đủ để khách tìm thấy và tin bạn.", items: ["Landing page chuyển đổi", "Tên miền năm đầu", "Email doanh nghiệp"] }, { name: "Ra mắt", price: "8.900.000đ", text: "Bộ mặt chỉn chu cho thương hiệu đang lớn lên.", items: ["Website 5 trang", "Tên miền năm đầu", "5 hộp thư doanh nghiệp"], featured: true }, { name: "Đi xa hơn", price: "14.900.000đ", text: "Một nền tảng bán hàng có thể tiếp tục phát triển.", items: ["Website bán hàng", "Cấu hình theo yêu cầu", "Bàn giao và hướng dẫn"] }];
export default function PackageCards() { const [selected, setSelected] = useState("Ra mắt"); return <div className="package-grid">{packages.map((item) => <article className={`package-card ${item.featured ? "featured" : ""} ${selected === item.name ? "selected" : ""}`} key={item.name}>{item.featured && <span className="package-badge">Được chọn nhiều</span>}<p className="package-kicker">Gói {item.name}</p><h3>{item.price}</h3><p>{item.text}</p><ul>{item.items.map((x) => <li key={x}>{x}</li>)}</ul><button type="button" onClick={() => setSelected(item.name)}>{selected === item.name ? "Đã chọn" : "Chọn gói này"}</button></article>)}</div>; }
