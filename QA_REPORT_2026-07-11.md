# Một Ngụm — QA & redesign report

## Mục tiêu của bản này

- Bỏ logo ảnh cũ để loại dấu chấm thừa và lỗi raster.
- Dùng wordmark tạo bằng HTML/CSS, hỗ trợ tiếng Việt ổn định.
- Tách nền bốn ảnh ly cà phê và chuẩn hóa thành bốn frame WebP trong suốt.
- Tạo chuyển động cốc cà phê bằng CSS, không thêm thư viện animation và không chặn nội dung lúc tải.
- Rà lại nội dung chatbot AI theo hướng minh bạch, không hứa quá khả năng.
- Giữ toàn bộ router dịch vụ hiện tại.

## Thay đổi chính

### Logo

- Component mới: `components/BrandMark.tsx`.
- Không còn dùng `logo-wordmark.png`.
- Không có chấm tròn thừa phía trên chữ.
- Logo dùng Georgia/Arial hệ thống để không phải chờ font bên ngoài.
- Hạt cà phê được vẽ bằng SVG nhỏ giữa hai đường kẻ.

### Hero cà phê

- Component mới: `components/CupTurntable.tsx`.
- Bốn frame đã được cắt nền và chuẩn hóa:
  - `public/assets/hero-frames/cup-1.webp`
  - `public/assets/hero-frames/cup-2.webp`
  - `public/assets/hero-frames/cup-3.webp`
  - `public/assets/hero-frames/cup-4.webp`
- Tổng dung lượng bốn frame khoảng 390 KB.
- Frame đầu tải ưu tiên; ba frame sau tải bổ sung.
- Chuyển động là CSS crossfade nhẹ, có `prefers-reduced-motion`.
- Nội dung văn bản không bị ẩn trong lúc chờ JavaScript.

### Font và bố cục

- Heading/logo: `Georgia, Times New Roman, serif`.
- Nội dung/UI: `Arial, Segoe UI, sans-serif`.
- Không gọi Google Fonts hoặc font CDN.
- Giảm cỡ heading cực lớn, cân lại khoảng trắng và card.
- Tối ưu responsive ở 980 px, 820 px và 520 px.

### Minh bạch về AI

Trang `/dich-vu/chatbot-ai` có thêm sáu nội dung:

1. AI có thể trả lời sai.
2. Dữ liệu cần được bảo vệ.
3. Phản hồi có thể chậm.
4. Không nên phụ thuộc vào nội dung nằm trong ảnh.
5. Nội dung AI cần người biên tập và kiểm tra nguồn.
6. Chatbot không thay thế sự thấu cảm và người phụ trách.

Mỗi mục có phần “Cách xử lý” tương ứng, tránh diễn đạt như cam kết tuyệt đối.

## Kiểm tra đã thực hiện

- `npm run lint`: đạt, TypeScript không báo lỗi.
- Development server khởi động thành công với Next.js 15.5.12.
- Các router chính trả về HTTP 200 sau khi compile:
  - `/`
  - `/menu`
  - `/qr`
  - `/dich-vu`
  - `/dich-vu/chatbot-ai`
  - `/tu-van-marketing`
  - `/bang-gia`
  - `/ve-mot-ngum`
  - `/lien-he`
- Kiểm tra HTML xác nhận bốn frame cốc và sáu thẻ giới hạn AI đều được render.

## Lưu ý môi trường kiểm thử

Lệnh `next build` đã compile mã nguồn thành công nhưng tiến trình trong container kiểm thử bị treo tại bước `Collecting page data`. TypeScript và toàn bộ router trên development server đều đã chạy được. Trước khi đưa production, nên chạy lại trên Windows/Vercel:

```powershell
npm install
npm run lint
npm run build
```

Vercel dùng môi trường build riêng nên không phụ thuộc giới hạn worker của container kiểm thử này.
