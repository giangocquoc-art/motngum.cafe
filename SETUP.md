# Setup

Requires Node 22+ and npm. Run `npm install`, copy `.env.example` to `.env.local`,
then run `npm run dev`. The app is a Next.js App Router application; all credentials
belong in server-side environment variables.
